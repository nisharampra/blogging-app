<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Character set and viewport settings -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Title of the page -->
    <title>Edit Article</title>
    <!-- Link to external stylesheet for custom styles -->
    <link rel="stylesheet" href="/styles.css">
    <!-- Link to Milligram CSS framework for styling -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/milligram@1.4.1/dist/milligram.min.css">
</head>
<body>
    <!-- Main heading for the page -->
    <h1>Edit Article</h1>
    <!-- Form for editing an existing article -->
    <form action="/articles/edit/<%= article.id %>" method="POST">
        <fieldset>
            <!-- Label and input for the article title with pre-filled value -->
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= article.title %>" required>

            <!-- Label and textarea for the article content with pre-filled value -->
            <label for="content">Content:</label>
            <textarea id="content" name="content" rows="10" required><%= article.content %></textarea>

            <!-- Label and input for the published date with pre-filled value, set to readonly -->
            <label for="publishedDate">Date Added:</label>
            <input type="text" id="publishedDate" name="publishedDate" value="<%= new Date(article.created_at).toLocaleDateString() %>" readonly>

            <!-- Submit button to save changes to the article -->
            <button type="submit">Save Changes</button>
        </fieldset>
    </form>
</body>
</html>
